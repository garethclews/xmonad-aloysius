#!/usr/bin/env zsh

# suppress "zsh no matches found"
setopt +o nomatch
autoload -U age

AGEREF=$(stat -c %y ~/.xmonad/xmonad-x86_64-linux)
OLD=$(ls *(+age) | grep -E '*.hs' | wc -l)

[[ $OLD > 0 ]] && ./compile > /dev/null
