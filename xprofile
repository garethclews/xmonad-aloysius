# Keys etc.
export LANG=en_GB.UTF-8 &

# turn on useful keys
numlockx &

# check for and load .Xresources
[[ -f ~/.Xresources ]] && nohup xrdb -merge -I$HOME ~/.Xresources >/dev/null

# compositing
compton -c ~/.config/compton/compton.conf &

# services
# gnome-session &
